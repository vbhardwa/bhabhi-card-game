package com;

import java.util.LinkedList;
import java.util.ListIterator;

public class Test {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*LinkedList<String> list = new LinkedList<String>();
		list.add("a");
		list.add("b");
		list.add("c");
		list.add("d");
		
		LinkedList<String> temp = new LinkedList<String>();
				
		ListIterator<String> it = list.listIterator();
		while(it.hasNext()){
			String s = it.next();
			if (s.equals("c")) {
				break;
			}
			temp.add(s);
			it.remove();
		}
		
		it = temp.listIterator();
		while(it.hasNext()){
			String s = it.next();
			list.add(s);
			it.remove();
		}
		
		for (String s : list) {
			System.out.println(s);
		}
		
		Card c = new Card ();
		c.setSuite(SuiteType.CLUBS);
		c.setValue(CardValue.NINE);
		
		Card c2 = new Card ();
		c2.setSuite(SuiteType.CLUBS);
		c2.setValue(CardValue.NINE);
		
		SuiteType s = SuiteType.CLUBS;
		SuiteType s2 = SuiteType.CLUBS;
		
		CardValue v = CardValue.NINE;
		CardValue v2 = CardValue.NINE;
		
		if (c.equals(c2)) {
			System.out.println("Yes, the cards are equivalent");
		} else {
			System.out.println("No, the cards are not equivalent");
		}
		*/
		
	}

}
